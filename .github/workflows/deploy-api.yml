name: Build & Deploy R API

on:
  push:
    branches: [main]
    paths:
      - "api/**"
      - ".github/workflows/deploy-api.yml"

concurrency:
  group: deploy-api
  cancel-in-progress: false

jobs:
  build-and-deploy:
    name: Build image and deploy to Fly.io
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install flyctl
        run: curl -fsSL https://fly.io/install.sh | sh

      - name: Destroy existing machines (clear crash loop)
        run: |
          export PATH="$HOME/.fly/bin:$PATH"
          MACHINES=$(flyctl machines list --app rpact-api --json 2>/dev/null | python3 -c "
          import json,sys
          data=json.load(sys.stdin)
          print(' '.join([m['id'] for m in data if m.get('state') != 'destroyed']))" 2>/dev/null || echo "")
          for m in $MACHINES; do
            echo "Destroying machine $m"
            flyctl machines destroy "$m" --app rpact-api --force 2>&1 || true
          done
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Build locally and deploy to Fly.io
        # Build on GitHub Actions (7GB RAM, no timeout) then push directly to Fly.io
        # This avoids GHCR auth issues - flyctl handles the image transfer itself
        run: |
          export PATH="$HOME/.fly/bin:$PATH"

          # Build the image locally on the GH runner
          docker build --platform linux/amd64 -t rpact-api:local ./api

          # Use flyctl to deploy from local image (flyctl pushes it to Fly.io registry)
          flyctl deploy \
            --config api/fly.toml \
            --local-only \
            --image rpact-api:local \
            --ha=false \
            --detach \
            2>&1 || true

          echo "Deploy triggered."
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
